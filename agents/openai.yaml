interface:
  display_name: "大学PPT逐页备稿"
  short_description: "依据真实PPT分批生成并审校大学课程逐页授课备稿"
  default_prompt: "使用 $university-ppt-notes。先用 ppt-speech-writer 读取并渲染整套PPT，向我提交全讲内容、逻辑关系、视觉重点和详略规划供确认；确认后由总Skill按页面教学任务逐页独立成文，完成5页后再联合检查，并只用 humanize 做轻量表达检测。"

policy:
  allow_implicit_invocation: true
