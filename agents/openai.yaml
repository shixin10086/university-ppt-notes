interface:
  display_name: "大学PPT逐页备稿"
  short_description: "依据真实PPT分批生成并审校大学课程逐页授课备稿"
  default_prompt: "使用 $university-ppt-notes，并调用 ppt-speech-writer 与 humanize。先读取并渲染整套PPT，提交教学主线、页面组逻辑、视觉重点和详略规划供确认；确认后再每5页生成逐页备稿，并用一次合并式检查完成必要的自然度与教师口述修正。"

policy:
  allow_implicit_invocation: true
