---
name: university-ppt-notes
description: 为现有中文大学课程PPT撰写、审阅和交付逐页授课备稿。先完成整套PPT视觉取证与教学梳理，再按五页一批生成自然、准确、详略得当的教师讲稿，经用户确认后写入累计稿，最终导出Word并写入PPT备注。
---

# University PPT Notes

## 目标

把真实PPT转化为可以直接讲授的逐页备稿。内容来源只取PPT本身和用户在当前对话中的明确要求；不要主动读取同目录的Word、PDF或其他说明文件。

专业准确性和教学清晰度优先。口语化体现为句间连贯、听觉顺序自然和解释容易理解，不体现为聊天化措辞、口头禅或随意改写专业含义。

## 开始前

完整读取：

1. `references/authoring-rules.md`
2. `references/guided-workflow.md`
3. `references/companion-skills.md`

向用户显示伙伴Skill检查卡：

- `ppt-speech-writer`：需要另行安装，用于PPT提取、渲染、视觉复核和备注写入。
- `humanize`：需要另行安装，用于发现并最小化修正AI式表达。
- 教师口述检查：属于本Skill内部流程，无须另行安装。

缺少伙伴Skill时应明确说明，不能悄悄降级。随后用一张确认卡核对处理范围、授课对象、字数口径、正文四档范围、已知重点或简讲页及交付形式。用户可回复“全部按默认”。

## 工作流

### 1. 整套取证与分析

调用 `ppt-speech-writer` 读取并渲染整套PPT，覆盖全部页面。需要时读取 `references/deck-analysis-report.md`，向用户提交完整的全讲分析：教学主线、章节和页面组逻辑、关键视觉、详略建议、重复内容处理以及首批页面的位置。

用户确认全讲分析前，不生成P01—P05。

### 2. 五页一批生成

每批查看前5页、当前5页和后5页；首尾不足时使用现有页面。先确认当前批次应包含的准确页码，再为每页确定教学任务、可靠事实、必要视觉、与前后页的关系和详略档位。

读取当前五页及前后文后，先逐页完成写作前规划，再开始生成讲稿。判断页面属于文字页、示意图页、图表页、图文混合页还是过渡页；区分PPT已经清楚呈现、不应同义复述的内容，需要教师解释的关系、原因、机制或实际含义，以及必须结合画面讲解的视觉信息。

普通文字页主要讲页面背后的逻辑与意义；示意图、流程图和数据图表页围绕观看顺序、对象关系、变化过程和结论进行讲解。

按 `references/authoring-rules.md` 直接写成教师讲稿。只按需查看 `examples/page-type-examples.md` 中与当前页同类型的一例，不得照搬课程内容。

### 3. 一次合并式语言检查

生成后读取：

- `references/humanize-protocol.md`
- `references/teacher-oral-delivery.md`

Humanize和教师口述检查合并为一次复核，不各自重写整稿。只修改有明确问题的句子或段落；若内容已经准确、自然、可讲，立即停止修改。

送审前在同一轮复核中执行局部异常扫描，检查词语搭配、概念归类和单句信息密度。只有能够明确指出具体问题时才修改，并只处理造成问题的最小片段；不得把偶然出现的问题扩展成禁用词或通用句式限制。

### 4. 展示、确认和写入

在聊天中完整展示当前批次。提交前核对实际页码集合与预期页码集合完全一致，不得缺页、重页或跳页。

用户确认前，不得把草稿写入唯一累计稿。用户回复“通过”或“下一步”时，才把刚刚展示的完整批次写入累计稿并继续；若用户提出修改，先重写并再次展示。

### 5. 全稿检查与交付

全稿生成完毕后读取 `references/final-audit.md`，先列出需要修改的页面供用户审核，再集中返修。只有怀疑出现某类语言问题或用户要求诊断时，才读取 `references/quality-benchmarks.md`；它不是生成前检查表。

最终从同一份已确认累计稿生成：

- Markdown累计稿；
- Word展示版；
- 写入备注栏的PPT副本。

PPT备注中只保留本页的【引入】【备稿】【收束】等实际讲授内容，不写页码、标题、字数、档位或审核说明，不覆盖原PPT。

## 不可妥协的边界

- PPT是课程事实的唯一文件来源，不补造PPT没有的信息。
- 整套分析必须先于逐页写作，并由用户确认。
- 每批必须页码完整，用户确认后才能写入累计稿。
- 格式与字数边界按已确认设置执行。
- 真实流程图、原理图和示意图需要服务于知识解释；普通照片和排版元素不必逐项描述。
- 不用设问、自问自答、口头禅和聊天化表达。
- 不为满足规则而改坏原本准确、流畅的句子。
